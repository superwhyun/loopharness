# Antigravity CLI (agy) Supplement

이 저장소의 canonical 프로젝트 지침은 `AGENTS.md` 이다.

Antigravity CLI (agy)에서는 아래를 기준으로 사용한다.

1. `AGENTS.md`
2. `skills/harness/framework/docs/HARNESS.md`
3. `.agy/commands/harness.toml`, `.agy/commands/review.toml`

프로젝트 명령:

- `/harness`
- `/review`
- `/loop`

배치 실행기 `skills/harness/framework/scripts/execute.py` 는 선택 사항이다.
기본 사용 방식은 agy가 이 저장소의 문서를 읽고 인터랙티브하게 작업하는 것이다.
